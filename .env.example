# Copy to `.env.production` before building for the server.
#
# Where the Django API is reachable from a visitor's browser — including the
# /api suffix. Leave it unset for local development and Vite proxies instead.
VITE_API_BASE_URL=https://api.example.dz/api
